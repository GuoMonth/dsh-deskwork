# 路线图

当前阶段：M1 实现与预览交付，真实森果验收待完成。[M1 规格](specs/m1-senguo-workspace.md)是本轮范围的事实来源；下方原始能力顺序继续表达长期路线。

## 本轮 M1：大步骤交付

- [x] 确定森果档口批发、macOS arm64、浅色商务视觉及两种视图。
- [x] 统一 Token、组件与交互预览；实现桌面工作台。
- [x] 发布版 DSH 与宿主工具桥验证；Electron CDP 夹具读写闭环。
- [x] 确认、停止、响应丢失核对与技能适用性回归。
- [ ] macOS 安装包构建、独立启动与 CI 验收。
- [ ] 测试店铺登录后固化真实字段适配，完成 DeepSeek + 森果写入和恢复。
- [ ] 用户 Mac 完成安装、登录、重启与业务验收。

M1 预览提前承接原路线的轻量 Agent 界面与单平台打包；API、通用 MCP 和自动进化仍在后续。

## 0. 定义项目

- [x] 确定 DSH Deskwork 名称与产品定位。
- [x] 编写中英文 README、设计说明与 MIT 许可证。
- [x] 建立 AI coding 规范、强类型检查、统一格式与分层证据目录。
- [ ] 选择首个目标 ERP、测试账户与无敏感数据的验证流程。
- [ ] 确认 DSH 上游、集成版本与分发许可证要求。
- [ ] 验证桌面浏览器方案并确定首个目标操作系统。

验收：明确一个可测试的业务目标、技术选择与 DSH 集成方式。

## 1. Copilot 最小闭环

- [ ] 桌面壳、单个业务标签页和右侧对话区。
- [ ] 用户手动登录并保持会话。
- [ ] 集成 DSH，通过工具读取选定页面并完成查询任务。
- [ ] 展示执行步骤，支持暂停、接手和重新读取页面状态。
- [ ] 完成一条辅助填写流程，提交前展示内容并确认。

验收：在一个 ERP 中，用户登录后通过对话完成查询，以及一条经确认的写入流程；能在业务页面核对结果。

## 2. Agent + 业务 API

- [ ] 提供以对话为主的 Agent 界面。
- [ ] 切换模式保留任务上下文和明确的会话绑定。
- [ ] 为一个已验证的业务动作实现 API 工具。
- [ ] 登录过期时提示用户重新认证并恢复任务。
- [ ] 覆盖请求超时后的结果核对，避免重复业务写入。

验收：同一任务可以从 Copilot 切换到 Agent，通过选定会话的业务 API 完成并回读结果。

## 3. Agent + MCP

- [ ] 提供 MCP 连接与凭据配置。
- [ ] 支持连接检查、工具发现和工具范围选择。
- [ ] 支持只配置 MCP、不依赖 ERP 网页登录的任务。
- [ ] 展示工具执行结果与失败原因。

验收：用户配置一个 MCP 服务后，可在 Agent 模式独立完成一个业务任务。

## 4. 桌面预览版

- [ ] 整理已验证业务流程与连接器说明。
- [ ] 补充安装、开发、配置与故障排查文档。
- [ ] 检查凭据处理、第三方许可证与发行包内容。
- [ ] 发布首个目标操作系统的预览安装包。

验收：新用户可以按文档安装，连接受支持系统，并完成示例流程。
